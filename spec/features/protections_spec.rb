require 'rails_helper'

RSpec.describe "Protections", type: :feature do
    #let!(:protections) { create(:protection, user: user) }
    # let!(:user) { create(:user) }

  #scenario "対象のデータが表示されること" do
     # binding.pry
  # expect(page).to have_content protections.protection_title
  # expect(page).to have_content protections.protection_breed
   #expect(page).to have_content protections.protection_choker
   #  expect(page).to have_content protections.protection_profile
   # expect(page).to have_content protections.protection_place
   #  expect(page).to have_content protections.protection_date
   #  expect(page).to have_content protections.protection_contactv
   #  expect(page).to have_content protections.protection_dogtag
   # end

   # scenario "保護犬詳細ページから保護犬一覧ページへのリンクが機能していること" do
   #click_link '一覧ページへ戻る'
   # expect(current_path).to eq protections_index_path
   #end
end
